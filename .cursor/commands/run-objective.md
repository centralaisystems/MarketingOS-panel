# Run marketing objective

Use the Marketing Director for a single brand.

```bash
pnpm run-objective -- --brand LOTIN --objective "Prepare an investor acquisition campaign."
```

Brands: `LOTIN` | `VILLA_GLORY` | `NOX_FORM` | `NOX_TECH`

Expected: load one brand, create tasks, specialist results, Guardian, no external side effects.
